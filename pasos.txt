1. al inciiar proyecto ejecutar:
 npm init -y

2. instalar sass como dependencia de desarrollo
npm install sass -D

3. Agregamos un script al package.json, reemplazando a test
"scripts": {
    "sass": "sass --watch scss:assets/css"
  },